# Project Agent Instructions — Boris Cherny Workflow (ChernyCode)

This workspace follows Boris Cherny's productivity guidelines:

## Core Workflow Rules
1. **Planning**: Start complex tasks with a clear plan before modifying code.
2. **Verification**: Always run `npm run build` or automated tests before claiming a task is done.
3. **Continuous Memory**: Record any corrections in this file or project rules so they persist across sessions.
4. **Simplification**: Run `/code-simplifier` or clean up after completing features.
5. **Subagents**: Use `code-reviewer`, `verifier`, `test-writer`, and `doc-generator` for parallelized execution.

## User Preferences & Memory
- **GitHub Account**: Always use `mahdi2983` for all repositories, commits, and pushes (never `MahdiHellali`).
